
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="row " id="employee-form">
    <div data-bind="if: !isViewDetail()">
        <!--ko template: viewListTemplate -->
        <!-- /ko-->
    </div>

    <div data-bind="if: isViewDetail()">
        <!--ko template: viewDetailTemplate -->
        <!-- /ko-->
    </div>

</div>

<script type="text/html" id="employee-Detail">
    @Html.Partial("_Detail")
</script>

<script type="text/html" id="employee-List">
    @Html.Partial("_List")

</script>

<script src="~/Scripts/jquery-2.2.4.min.js"></script>
<script src="~/Scripts/apps/Employee/ko-employee.js"></script>
<script src="~/Scripts/apps/Employee/EmployeeModel.js"></script>

<script type="text/javascript">
    var builder = null;


    $(document).ready(function () {
        if (typeof (CommonEnum.API_URL == 'undefined'))
        {
            CommonEnum.API_URL = {};

        }
        CommonEnum.API_URL = $.extend({}, CommonEnum.API_URL,
            {
                GetEmployee: '@Url.Action("GetEmployees", "Employee", new { Area = "admin" })',
                GetEmployeeDetail: '@Url.Action("GetEmployeeDetail", "Employee", new { Area = "admin" })',
                UpdateEmployee: '@Url.Action("UpdateEmployee", "Employee", new { Area = "admin" })',
                InsertEmployee: '@Url.Action("InsertEmployee", "Employee", new { Area = "admin" })',
                DeleteEmployee: '@Url.Action("DeleteEmployee", "Employee", new { Area = "admin" })',

                GetHistory: '@Url.Action("GetHistory", "Home")',
            });

        var form =
        {

        };
        var builder = new EmployeeListFormViewModel(form);
        ko.applyBindings(builder, document.getElementById('employee-form'));
    });

</script>

